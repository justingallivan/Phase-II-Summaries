# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.tgz
*.tar.gz

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local

# Vercel
.vercel

# IDE
.vscode/
.idea/

# Source documents
*.docx
# Migration data files
proposals-for-migration.csv
# Generated schema output (may contain tokens)
scripts/dynamics-schema-output.json
scripts/dynamics-schema-diff.json
.git.nosync
tmp/
.playwright-mcp/
# Codex CLI auto-generated artifacts — blind s/Claude/Codex/ derivative of
# CLAUDE.md / .claude/skills (asserts false stack + unsafe VRP allowlist value).
# .agents/ stays ignored. AGENTS.md is now a COMMITTED thin pointer to CLAUDE.md
# (not ignored) — if Codex tooling re-clobbers it with the full derivative, that
# shows as a tracked-file diff; restore with `git checkout AGENTS.md`.
# See docs/DOCS_GROUND_TRUTH_AUDIT_2026-05-19.md.
.agents/
