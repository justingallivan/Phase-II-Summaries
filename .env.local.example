# Local environment example for Entra ID (Azure AD) + NextAuth
# Copy this file to .nenv.local and fill in values before running locally

# NextAuth settings
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Azure AD / Entra configuration
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=

# Optional: when using Vercel Postgres from @vercel/postgres
DATABASE_URL=
