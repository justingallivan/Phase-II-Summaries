---
name: Dataverse Power Tools — two separate apps (Find&fix + Bulk export)
description: Scoped plan to fill the two gaps Dynamics Explorer can't (targeted field edits; high-volume filtered export) — both currently absorbed by Akoya Go
type: project
originSessionId: S156
---
Two separate apps, one spine. Dynamics Explorer covers "most users, simple question" but structurally cannot do two things still done in Akoya Go (poor UX): **Track A** = maintenance staff find a record and edit a field; **Track B** = volume users pull filtered data beyond the Explorer's agentic cap (trigger: CSO ask for ~5,000 requests as Excel). Shared spine = AI proposes → deterministic code performs → human-confirm gate (compiler-not-interpreter, the opposite of Explorer). **Full design, all evidence, and `[VERIFIED file:line]` claims live in `docs/DATAVERSE_POWER_TOOLS_DESIGN.md` — read it before any build plan; this entry is orientation only.**

**Why:** Track B's naïve "filter→page→Excel" framing is wrong. `akoya_request` (~25,561 rows) is **polymorphic** ("grant" is a view: research grants, feedback-only concepts, SoCal, discretionary, $100M projects) and **temporally polymorphic** (clean Akoya-native vs. approximate migrated). Completeness = property of (field × era × program). Users are PhDs/lawyers/MBAs who own their analyses — optimize against the *plausible* wrong answer that passes the sniff test. Fits [[project_intake_portal_skinny_scope]]; on the GOapply-replacement arc.

**Status (S158):** Design converged; all 4 data puzzles Codex-signed-off (S157). **Residual (i) — AkoyaGo export-column SET: ✅ CLOSED.** The "user excavation, not solo-actionable" framing was wrong (AkoyaGO *is* the probed Dataverse env — views/reports are first-class records). Three read-only probes delivered it solo: `scripts/probe-akoya-saved-views.js` (full surface, 0 privilege gaps; userquery=0 + doc-templates junk ⇒ trusted exports are *system* views/reports; 139 program-segmented public views + ~60 grant reports), `scripts/probe-akoya-report-defs.js` (20/20 trusted RDLs → FetchXML), `scripts/probe-akoya-test-record-predicate.js` (residual iii). Dated evidence: `docs/atlas/evidence/akoyago-saved-views-2026-05-16.txt`, `akoya-report-defs-2026-05-16.txt`, `akoya-test-record-predicate-2026-05-16.txt`. Step D done: design doc "Artifact 1 — Trusted Export-Column SET" = canonical bucket-annotated export-column SET + operational filter patterns; key finding = bespoke RDLs have NO hardcoded filter (`enableprefiltering`) ⇒ trusted export = stable column contract + operator-supplied filter (validates Track B design). Independent cross-confirms: decisiondate time-slice, decline structured→free-text era-relocation (Connor's own "SoCal Application Log II" adds native `wmkf_denialnotes`). **Residual (iii) ✅ CLOSED S158** (see test-rows bullet — predicate validated, both money-bearing swept rows user-confirmed test). **Net before build plan: ONLY (ii)** — the small Connor semantic sign-off (now also carrying the surfaced `wmkf_grantprogram`-filter-vs-`akoya_programid`-display nuance); one folded session. Track A write path separately gated on its 3 explicit decisions. Slice-0 soft-dated separately — see [[project_slice0_timeline_posture]].

**How to apply:**
- Track A: populated-fields-only inline edit + single Dataverse Search box. v1 single-record, safe scalars only, optionset/lookup read-only. **Write path gated on 3 Codex-surfaced decisions:** (1) `MSCRMCallerID` attribution NOT guaranteed (env-gated, falls back to service principal) — fail-closed/warn; (2) `updateRecord` skips `checkRestriction` — enforce explicitly; (3) `IsValidForUpdate=true` insufficient — exclude calculated/rollup/formula via SourceType/AttributeOf. Read/display path unblocked.
- Track B: NOT a generic builder, NOT fixed templates — a plain-English structured filter builder (fan-out choices forced) + mandatory composition/era/classification/program disclosure even in expert mode + trust-bounded export with class-aware sentinels (never bare blanks). AI = phase-2 on-ramp for non-experts, not the core. **Export engine MUST use FetchXML aggregate / RetrieveTotalRecordCount, never OData `$count`** (caps silently at 5,000 — this undercount IS the trigger). Parameterize the arbitrary 5,000 cap; surface `capped`/`totalCount`.
- **Era classifier (exact, external-dependency-free, S157 dated evidence):** `createdon`=2023-12-03 ⇒ migrated (22,573, Blackbaud/"Sky"→AkoyaGO cutover, single ~43-min import window); `createdon`>2023-12-03 ⇒ Akoya-native (2,988). 🔴 **Exact as a CREATION-PROVENANCE partition only — NOT a business-era one** (`scripts/probe-akoya-codex-substantiation.js` C5): 168 native rows (18% of dated native) have `akoya_decisiondate`<2024, 169 (5.7% of native) some pre-2024 business date. Mandatory rule: time-slice history on `akoya_decisiondate` (corroborated by `wmkf_meetingdate`), NEVER `createdon`; treat migrated/native as provenance, not period; disclose the ~169-row cross-contamination when both dims appear. Creation-provenance findings (backfill/net-new/BB-lineage) are unaffected.
- **Decided-state predicate = `akoya_requeststatus` value→class map** (Pending\*=in-flight; \*Declined/Ineligible/Denied/Closed/\*Done/Approved=decided-terminal). NOT decisiondate-presence (that is an *approval* stamp — substantiated, not assumed). Ambiguous middle: **`Active` ✅ CONFIRMED (user, S158) = awarded grant in active performance period (decided:yes, closed:no); no in-flight reading exists.** `Proposal Not Invited` ✅ CONFIRMED (user, S158) = staff declined to invite a full proposal — terminal decline, no award (→ declined class). `Withdrawn` ✅ CONFIRMED (user, S158) = terminal, no award, own class — **PATH-AGNOSTIC: do not attribute cause (applicant backed out OR administrative withdrawal); Track B sentinel must not name an actor**. **All 3 ambiguous-middle labels now closed — status sign-off is no longer a Connor residual.** S158 instance pull (`scripts/probe-akoya-active-nodate.js`, evidence `docs/atlas/evidence/akoya-active-nodate-2026-05-16.txt`): the 7/109 native `Active`-with-no-`akoya_decisiondate` rows are 100% real fully-paid grants (clerical null, not ambiguity — mostly Special Grants/Misc handled off the regular board cadence). **Track B rule:** when `akoya_decisiondate` is null on an `Active`/awarded row, fall back to `wmkf_meetingdate` for the "when decided" axis. **Forwarded data-quality follow-up (user → Connor + Sarah, NOT a Track B blocker):** #1001205 (Project Angel Food) + #1001249 (The Harmony Project) — normal SoCal-area `Program` grants with null decisiondate, suspected genuine oversights.
- **Two durable design invariants** (both in the design doc as named sections): **Living taxonomy** — read live, fail loud; durable record = invariants/patterns/methods, NOT hardcoded value lists; counts are dated evidence only (see [[project_living_taxonomy_principle]]). **Process is program-scoped** — every program (research/SoCal/discretionary/…) runs its own application/review/advancement/documentation/decline process; whole-entity analysis of any process-dependent attribute is invalid by default; Track B must segment process-dependent output by program and fail loud on unannotated ones. Every S157 process finding is program-scoped, predominantly RESEARCH unless stated.
- **META-PATTERN (durable): every era-asymmetric field needs its OWN structural explanation — NO blanket "migrated-high = backfill" or "= real" rule.** Worked examples: `akoya_request`/`akoya_expenses` migrated-100% = migration backfill artifact (Track B must never export migrated values here as real amounts); `akoya_grant`/`akoya_originalgrantamount` = real lifecycle state (native in-flight **0%** vs **award-eligible decided 96%/100%** vs non-award terminal ~11%≈0-by-design — confound strongly confirmed; the earlier blended "decided-terminal 38%" was a mis-cut, retired — `scripts/probe-akoya-codex-substantiation.js` C3); `akoya_denialreason` = NOT backfill (decline-reason capture relocated structured→free-text across the migration: migrated `akoya_denialreason` Picklist, native `wmkf_denialnotes` Memo — "why declined" must be era-aware or a false 2023-12-03 cliff appears).
- **Discriminator is a correlated multi-axis set, not a flat composite and not strictly nested:** `wmkf_type` (Lookup, coarse top-level) × `akoya_programid` (Lookup→`akoya_program`, 24-program authoritative taxonomy, REQUIRED on form so most reliable program field — authoritative over 4,634-null `wmkf_grantprogram`) × `wmkf_request_type` (Picklist interaction-kind) × `akoya_requesttype`. `akoya_programid` is populated across most `wmkf_type` values (not nested under `wmkf_type=Program`). "Real grants" filter must strip non-grant operational buckets (SiteVisit/OfficeVisit/Phone, `Research Reviewer`) on EVERY axis. **Operational-vs-grant buckets ✅ CONFIRMED (user, S158):** interaction-log axes (Office/Site Visit/Phone; `wmkf_type=Site Visit` — memo-resident purpose, not structured-classifiable, no mitigation owed) excluded; `wmkf_type=Miscellaneous` = **real grants** (verified 52 rows/$3.35M — memberships/dues/contributions/disaster relief, include); `akoya_program=Research Reviewer`≡`wmkf_type=Individual` = **paid peer-reviewer honoraria via GOapply, NOT grants** (87 rows, all 2026, $250, person→`contact`). **Sharpest exclusion predicate: `wmkf_grantprogram = Honorarium`.** **Program roll-up ✅ CONFIRMED (user, S158 — Option B):** `akoya_programid` = canonical program attribution, but "grants by program" total = `wmkf_type=Program` rows ONLY; `Special Projects`/`Special Grants`/other non-Program giving modes report as their own separate lines, never folded in (Medical Research: Program $493.0M vs pooling +$310M/~39%, mostly 3 Special Projects ≈$277.8M — `scripts/probe-akoya-program-rollup-medical-research.js`). Mandatory per-program in/out disclosure line. Status-label + buckets + roll-up DONE; **only the `wmkf_grantprogram`-filter-vs-`akoya_programid`-display nuance (item 3) remains in (ii)**. Side-finding → [[project_reviewer_identity_fragmentation]].
- **Test rows in prod — residual (iii) ✅ CLOSED (S158).** "No general detector" SUPERSEDED: system view "Test Requests" → verified predicate **applicant `account.name`="W. M. Keck Foundation" ∧ AkoyaGO-native era** (catches #1000799; 22-row bounded blast radius; dated evidence `docs/atlas/evidence/akoya-test-record-predicate-2026-05-16.txt`, probe `scripts/probe-akoya-test-record-predicate.js`). Validated: **0 real grants swept** — only 2 of 22 carry an amount and **both user-confirmed test** (#1000799 $1; #1002807 = duplicate of real #993347 for testing). **Durable mechanism:** test rows = a **cloned real request with the applicant swapped to the Foundation** (explains realistic titles/amounts AND why the applicant=Foundation marker is reliable). Forward maintenance note (not an open gate): exclude-with-the-documented-22-row-boundary; revisit only if WMKF ever legitimately self-grants (low risk, flagged).
- Forward intent (user, durable): WMKF wants doc-resident knowledge (decline rationale, review writeups now in SharePoint Word docs) moved ONTO Dataverse tables — structured/legible/searchable. SharePoint docs ARE reachable via existing proven infra (`getRequestSharePointBuckets`→`graph-service.js`→`file-loader.js::extractTextFromBuffer`; Dynamics Explorer already find+downloads), but current capability is filename/title only — bulk DOCX→text is a real later engineering phase, not a near-free toggle. Track B doc-link surfacing = interim bridge. Research is NOT dropping Phase I — consolidating to a single-submission model where "Phase II" = status promotion on the same record (so "Phase II" is process-era-dependent; counting across that boundary conflates two things). See [[project_machine_legible_form_capture]].
- Audit/change-history (S156, analyzed at scale, 800 Akoya-native records): canonical method to classify editors = `systemuser` metadata (`applicationid` set ⇒ app/integration user) + explicit vendor-name exclusion (`Bromelkamp`=AkoyaGO vendor; name-regex alone missed `Bromelkamp Admin`). Raw frequency disconfirmed (app user dominates ~12,742); staff-attributed + content-field-shaped signal IS usable for Track A editor prioritization. Per-record `RetrieveRecordChangeHistory` works; aggregate `/audits` blocked (SP lacks `ReadAuditSummary`). Candidate Track A enhancement, NOT a v1 dependency (privilege fragility). Produced an empirical ~17-person maintenance-staff roster = Track A access-scoping artifact.
