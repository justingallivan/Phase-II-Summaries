---
name: Dataverse Power Tools — two separate apps (Find&fix + Bulk export)
description: Scoped S156 plan to fill the two gaps Dynamics Explorer can't (targeted field edits; high-volume filtered export) — both currently absorbed by Akoya Go
type: project
originSessionId: S156
---
Dynamics Explorer covers "most users, simple question" but structurally cannot do two things still done in Akoya Go (poor UX): Track A = maintenance staff find a record and edit a field; Track B = volume users pull filtered data beyond the Explorer's agentic cap (trigger: CSO ask for ~5000 requests as Excel). **Two separate apps, shared spine** (AI proposes → deterministic code performs → human-confirm gate; compiler-not-interpreter, the opposite of Explorer). Full converged design + Codex reconciliation + `[VERIFIED file:line]` claims: **`docs/DATAVERSE_POWER_TOOLS_DESIGN.md`** — read it before any build plan.

**Why:** Track B's naïve "filter→page→Excel" framing is wrong. `akoya_request` is **polymorphic** ("grant" is a *view*: research grants, feedback-only concepts, SoCal program, high-volume discretionary, $100M projects) and **temporally polymorphic** (clean Akoya-native vs. approximate migrated; "ever" is rhetorical, deep-history is out of v1 by design → routes to Track A/Explorer find-one). Completeness = property of (field × era). Users are PhDs/lawyers/MBAs who own their analyses — optimize against the *plausible* wrong answer that passes the sniff test, not naïve trust; provenance = reproducible methods, not defensive armor. Fits [[project_intake_portal_skinny_scope]]; on the GOapply-replacement arc.

**How to apply:**
- Track A: populated-fields-only inline edit + single Dataverse Search box. v1 single-record, safe scalars only, optionset/lookup read-only. **Write path gated on 3 Codex-surfaced decisions**: (1) `MSCRMCallerID` attribution NOT guaranteed (env-gated, 403 retries without it, falls back to service principal) — fail-closed/warn; (2) `updateRecord` skips `checkRestriction` — enforce explicitly; (3) `IsValidForUpdate=true` insufficient — exclude calculated/rollup/formula via SourceType/AttributeOf. Read/display path unblocked.
- Track B: NOT a generic builder, NOT fixed templates — a **plain-English structured filter builder** (fan-out choices forced, e.g. "Budget: awarded/requested/total") + **mandatory composition/era/classification disclosure even in expert mode** + generous trust-bounded export (incl. discriminator/era/resolved-institution cols + explicit `UNKNOWN — not captured` sentinel) → refine in Excel. AI = phase-2 on-ramp for non-experts, not the core. Backoff-hardened paging is v1 (Codex). Parameterize the arbitrary 5000 cap; surface `capped`/`totalCount` in UI (helper already returns them).
- Real Track B work = 3 evidence-derived semantic-layer artifacts (plain-English field dict w/ fan-out; type/era taxonomy + boundary; field-availability boundary for ~10-15 export cols). **Design converged but BLOCKED on 3 evidence tasks before build plan:** Claude→Dataverse probe (discriminator field/values/volumes/era split via `docs/atlas/dataverse-akoya-request.md`); user→AkoyaGo excavation (trusted-view operational filters + migration cutover date); Connor→recent-era taxonomy + remap history.
- Discriminator/era probe DONE (S156, `scripts/probe-akoya-request-discriminators.js`): `akoya_request` true count **~25,561** but OData `/$count` caps at **5,000** (silent ~80% undercount — this IS the trigger; export engine MUST use FetchXML aggregate/RetrieveTotalRecordCount, never `$count`). Discriminator is a **composite**: `wmkf_request_type` (Request 16,227 / Concept 3,273 / Office-Site-Visit+Phone ~5,268 non-funding logs) × `wmkf_grantprogram` (Research 8,500 / Discretionary 5,345 / **null 4,634** / Southern California 4,489 / …) × `akoya_requesttype` (Grant 25,473 / Scholarship 88). All user-described slices confirmed w/ volume. **Era boundary RESOLVED precisely from Dataverse alone (S157, 2026-05-16).** Two probes: (a) `scripts/probe-akoya-overriddencreatedon.js` — `overriddencreatedon` DISCONFIRMED as marker (null on 100%/0 of 25,561; import never set it; prior "inconclusive" was a `$count`-cap artifact). (b) `scripts/probe-akoya-createdon-2023.js` — day-level drill: **all 22,573 "2023" rows created on a single date, 2023-12-03, in one ~43-min window (17:42:10Z–18:25:32Z); zero native creates in 2023; 2,988 native = 2024+ (1,167+1,376+445); 22,573+2,988=25,561 ✓.** ⇒ **Native-vs-migrated classifier is exact and external-dependency-free: `createdon`=2023-12-03 ⇒ migrated; `createdon`>2023-12-03 ⇒ Akoya-native.** **CORRECTION (same day, supersedes the line above + commit `3c3044d`'s "true dates irrecoverable" claim — it over-generalized from a *system* field to all dates and was WRONG):** (c) `scripts/probe-akoya-era-field-shape.js` (n=1,200/cohort) — the prior system is **Blackbaud ("Sky")**; the 2023-12-03 import was the Blackbaud→AkoyaGO cutover. Core grant facts came across **~1:1 with true history**: `akoya_decisiondate` is **100% populated in migrated** with a clean 1955→2023 spread (zero pre-1954; Keck founded 1954 — internally consistent). `akoya_decisiondate` (corroborated `wmkf_meetingdate`) is the **reliable historical-year key** — NOT `createdon` (collapsed), NOT `akoya_datereceived` (sparse ~7%). A real "2021/2022/2023 request" cut IS possible (decisions 824/954/986). Era-shape: ~28 native-only fields = the GOapply/workflow layer (0% migrated *by nature*, not loss); `_wmkf_bbstatus_value`+`wmkf_bbstaffid` = Blackbaud-lineage columns (secondary migrated-cohort signal). **⚠️ Lifecycle/era confound:** big amount/decision gaps (mig ~100%/nat ~30–50%) = pipeline state (native rows disproportionately in-flight), NOT data-model change — disclosure layer must say so; do not read native sparsity as missing data. **Artifact 3 DELIVERED then CORRECTED after Codex review (S157, 2026-05-16)** — `docs/DATAVERSE_POWER_TOOLS_DESIGN.md` → "Artifact 3". Codex review was well-founded; 4 of 5 findings tested by follow-up probes that **overturned claims**: (i) `probe-akoya-era-robustness.js` proved the n=1,200 GUID-ordered sample BIASED in the migrated cohort (grant asc95/desc61; grantprogram asc58/desc99) ⇒ all rates re-derived as **EXACT full-cohort FetchXML aggregates** via new `probe-akoya-export-col-rates.js` (mig tot=22,573, nat tot=2,988); `probe-akoya-era-field-shape.js` demoted to field-discovery only. (ii) createdon-immutability EVIDENCED via full-cohort reconciliation (all 22,573 migrated createdon in the import window; native spreads 2024-26; 22,573+2,988=25,561 exact; overriddencreatedon=0) — NOT the 5-row demo (illustrative only); the "createdon not PATCH-updatable" platform behavior is asserted, NOT independently verified (pass-2 downgrade from "substantiated"); TZ concern moot — raw min/max are absolute instants. (iii) **Lifecycle confound is FIELD-SPECIFIC, not blanket** (stratification block c): CONFIRMED for `akoya_grant`/`akoya_originalgrantamount` (84 mig / native-decided 95% vs not-decided 3%) but **REFUTED for `akoya_request`/`akoya_expenses`** (100 mig / native decided≈not-decided≈46% = structural/migration gap, cause UNVERIFIED → Connor). Buckets split B-lifecycle vs B-structural; blanket "NOT a schema change" RETRACTED. (iv) decade-distribution now committed/reproducible. (v) **Bucket C "Blackbaud didn't capture this" RETRACTED** — exact rates far higher than biased sample (grantprogram 58→80, primarycontact 43→70, grant 95→84); projectleader (16/32) re-bucketed C→D. Doc contradiction fixed (decisiondate IS a Dataverse-resident true historical date 1955→2023; AkoyaGo needed only for type-taxonomy/remap + B-structural cause). Disclosure sentinels marked PROVISIONAL (need a defined decided-state predicate). Exact buckets: A measured ≥97% both (requestnum/requesttype/wmkf_request_type 97-99/requeststatus/statecode/applicantid 100-97/meetingdate/fiscalyear/paid) · B-lifecycle (decisiondate 100/31, grant 84/32, originalgrant 84/33) · B-structural (request 100/48, expenses 100/45) · C both-eras (grantprogram 80/99, primarycontact 70/77, title 46/65) · D sparse-both (begindate/enddate 35/~38, projectleader 16/32, datereceived 7/22) · E provenance. **Puzzle 1 (B-structural) RESOLVED (S157, `scripts/probe-akoya-request-by-type.js`, user-hypothesis test):** migrated `akoya_request`/`akoya_expenses` 100% is a MIGRATION BACKFILL ARTIFACT (100% even on Office-Visit/Phone-Call rows that can't have a budget) — Track B must NEVER export migrated values for these as real amounts. Native gap is mostly request-type mix (Concept=11%, feedback-only, no budget — user was right). Native `Request`-type 32%-no-ask FULLY RESOLVED (`scripts/probe-akoya-native-request-amount.js`): 577/582 `Approved`, 93% awarded, 98% paid ⇒ invited/discretionary giving, requested-amount N/A by design (NOT missing). Phase-split hypothesis (loirequestedamount) tested+refuted (LOI ⊂ akoya_request, 0 LOI-only). **Puzzle 1 has NO residual and needed ZERO Connor input.** Track B export rule: requested-amount nulls = class-aware sentinels (migration-backfill / feedback-request / invited-discretionary-award / not-captured), never bare blanks. Residuals now: (i) AkoyaGo export-col SET; (ii) one small Connor class-map sign-off (folds into type-taxonomy session). [(iii) dropped — done.] **S157 discriminator CORRECTION (`scripts/probe-akoya-field-dictionary.js` + user screenshot, record 1002804):** AkoyaGO "Type" = `wmkf_type` (Lookup→`wmkf_type`), NOT the `wmkf_request_type` Picklist — two distinct axes (`wmkf_request_type`=interaction-kind Request/Concept/OfficeVisit; `wmkf_type`=grant-type e.g. Discretionary). Fuller discriminator = `wmkf_type` × `wmkf_grantprogram` × `akoya_programid` × `wmkf_request_type` × `akoya_requesttype`. `akoya_programid` ("Internal Program", Lookup→`akoya_program`) is REQUIRED ⇒ most reliable program field (99/80), authoritative over 4,634-null `wmkf_grantprogram`. `wmkf_type`/`akoya_programid` distributions NOT yet probed (artifact-2/Connor gap). Field-dictionary (artifact 1) seeded by the probe (label→logical→type→target). **🔴 Test-record hazard:** 1000799 = user-confirmed TEST row in prod `akoya_request`; test rows exist live, Track B must exclude, NO general detector yet (open data-quality item). **Program-axis taxonomy RESOLVED (S157, `scripts/probe-akoya-program-taxonomy.js`, GUID-keyed):** `akoya_programid`→`akoya_program` (set `akoya_programs`) = 24 programs w/ volumes + creation timeline; distinct taxonomy from `wmkf_grantprogram` (akoya_program splits Undergrad Ed into Liberal Arts 861 + Sci&Eng 2,583; wmkf_grantprogram single Undergrad Ed 2,017 — NOT 1:1). 816 null (form-required *now* but legacy rows precede it — earlier "required⇒always" too strong). 3 Track B hazards: (1) dup name `Law and Legal Administration` ×2 (2023-11-30 Inactive/0 + 2024-02-15 Active/62 — clean data, filter-by-GUID hazard); (2) era-scoped programs (`Strategic Fund` 2024-08, `Disaster Relief` 2025-01, `Bridge Funding` 2025-09, `Research Reviewer` 2026-01 = 0 migrated; pre-creation grant can't be in them; `Undergrad Ed-Liberal Arts` legacy-retired, 0 native); (3) non-grant operational bucket `Research Reviewer` (87, polymorphism like Office-Visit). Taxonomy living (14 seeded 2023-11-30, 6 on 2024-02-15, then incremental). **Type axis RESOLVED (S157, `scripts/probe-akoya-wmkf-type-taxonomy.js`, first probe under Living-taxonomy policy):** discriminator is HIERARCHICAL not flat — `wmkf_type` coarse top-level (Program/Discretionary/SiteVisit/SpecialGrants/SpecialProjects/Individual/Misc); `wmkf_grantprogram`/`akoya_programid` finer, nested under `wmkf_type=Program`. Joint same-label only 21% = entirely the `Discretionary`×`Discretionary` cell (5,345, same population = Puzzle-1 invited/discretionary mode). **Pervasive-polymorphism = cross-axis INVARIANT:** every type-ish axis carries non-grant operational buckets (SiteVisit/OfficeVisit in wmkf_type & wmkf_request_type; ResearchReviewer in akoya_program ≡ `wmkf_type=Individual`, same Jan-2026 all-native 87-row cohort) — "real grants" filter must strip operational buckets on EVERY axis. wmkf_type: no dup-name, 159 null ⇒ "key-by-GUID" is a defensive default justified by akoya_program specifically, NOT universal. Connor artifact-2 residual now small = semantic validation of derived operational-vs-grant assignments + the wmkf_type→program roll-up. **Puzzle 2 (declines) RESOLVED (S157, `scripts/probe-akoya-decline-recording.js`):** decline-reason capture RELOCATED structured→free-text across the migration — migrated declined use Picklist `akoya_denialreason` 98%; native abandoned it (8%) for Memo `wmkf_denialnotes` 47%. Track B "why declined" must be era-aware (`akoya_denialreason` migrated / `wmkf_denialnotes` native) or false 2023-12-03 cliff. `akoya_denialreason` NOT backfill (0% on Approved both eras) — opposite of Puzzle-1's `akoya_request`. **META-PATTERN (durable): every era-asymmetric field needs its OWN structural explanation; NO blanket "migrated-high = backfill" or "= real" rule.** Also: ≤~50% native declines have a reason in either field (Akoya-era data-quality gap); historical-key bound — migrated declined=100% decisiondate vs native declined=10% (presence-on-declines era-dependent, dates still real). SoCal has own `wmkf_socalreasonsfordecline2` (Virtual) — corroborates SoCal-separate-process. ("migrated-cohort stratification" WITHDRAWN pass-2 — migrated is degenerately ~100% decided (decisiondate 100% migrated), so a decided/not-decided split can't exist there; that asymmetry IS the lifecycle finding.) **Codex pass 2 (S157):** findings tested not just softened — `akoya_originalgrantamount` added to stratification loop (decided 99%/not-3% ⇒ confirmed); `statecode` added to exact-rate probe (100/100, was doc-only); `wmkf_projectleader` probe label fixed C→D; future-row count committed (`wmkf_meetingdate` 4 / `decisiondate` 0, reproducible); decisiondate's own B-lifecycle row flagged tautological; not-null-vs-trim caveat added; residual-list contradiction (Artifact 3 vs Status §) reconciled to two. **Decided-state predicate RESOLVED (S157, `scripts/probe-akoya-status-predicate.js`):** non-circular predicate = `akoya_requeststatus` (String, 100% both cohorts), NOT decisiondate-presence. Pending family (`Phase I/II Pending`/`Concept Pending`/`Pending`, native ~474) = clean undecided signal (100% no-date, 0% leakage). KEY: this disproved decisiondate as a "decided" flag. Codex pass-3 forced the over-read to be probe-tested: status-CLASS × decisiondate cross-tab (native) — APPROVED 89% / CLOSED 100% carry a date vs DECLINED/INELIGIBLE 10% / CONCEPT DONE 13% / PENDING 0%; so decisiondate is an *approval* STAMP (substantiated, not softened). Terminal-decided-without-date = **1,490 EXACT** (summed by probe; supersedes the inferred ~1,583). Active=94%-date ⇒ funded-in-progress not in-flight; Withdrawn n=7/0%-date. Predicate = `akoya_requeststatus` value→class map (Pending*=in-flight; *Declined/Ineligible/Denied/Closed/*Done/Approved=decided-terminal); ambiguous middle (`Active`/`Withdrawn`/`Proposal Not Invited`) folds into the existing Connor type-taxonomy session, not a new task. Disclosure sentinels rebuilt on the status class map. `statuscode`/`statecode` are useless (system Active/Inactive only). Folded into Atlas + design doc + S157 Codex-reconciliation section (pass-1 + pass-2 + decided-predicate bullets).
- Audit/change-history (S156, Codex-reviewed, then ANALYZED): capability probe `scripts/probe-dataverse-audit-capability.js` (aggregate `/audits` blocked — app SP lacks `ReadAuditSummary`; per-record `RetrieveRecordChangeHistory` works). Then the sampled analysis was actually run — `scripts/analyze-akoya-request-change-of-state.js` → `scripts/analyze-akoya-request-staff-edits.js`, 800 Akoya-native records (`createdon>=2024`), 800/800, no throttle. **Canonical method (reuse this): classify changing users via `systemuser` metadata — `applicationid` set ⇒ app/integration user (definitive) — plus explicit vendor name exclusion (`Bromelkamp`=AkoyaGO vendor, `# ` app users, `akoyaGO`/`integration`); name-regex alone missed `Bromelkamp Admin`.** Findings: raw frequency **disconfirmed** (`# BCO akoyaGO Integration` app user ≈12,742 changes dominates); but **staff-attributed + content-field-shaped** signal (nonstaff≈0, free-text/decision/classification: `wmkf_denialnotes`, `wmkf_conceptpapernotes`, `wmkf_wmkfprojectdescription`, `akoya_recommendedamount`, `akoya_title`, `akoya_programid`/`wmkf_grantprogram`/`akoya_payee`) IS usable for editor prioritization vs. lifecycle/status churn. Per-record point-of-edit history validated at scale → candidate Track A enhancement (NOT a v1 dependency — privilege fragility). Produced an empirical **WMKF maintenance-staff roster** (Hassas/Seeley/Noda/Hibler/Kwan/Garacochea/Gage/Stone/Pallais/Cumming/… ≈17 people) = Track A access-scoping artifact. Staff edit `wmkf_grantprogram` 92× ↔ Track B 4,634-null-program hole.
